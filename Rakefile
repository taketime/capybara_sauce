# start up sauce-connect
task :setup_sauce_connect
  puts "About to set up Sauce Connect"
end

# run tests
task :run_sauce_tests =>[:setup_sauce_connect]
  # set up tests
  # browsers = ENV['browsers']
  # oss = ENV['oss']

end
